#include<bits/stdc++.h>

using namespace std;

int main(){
  int n,t;
  cin >> n >> t;
  int d[t], a[n];
  for(int i = 0;i < n;i++) cin >> a[i];
  string s;cin >> s;
  for(int i = 0;i < s.length();i++){
    if(s[i] == 'B'){
      for(int j = i;j < n;j++){
        
      }
    }
  }
}